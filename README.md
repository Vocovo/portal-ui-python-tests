# GCUITEST

```
Required .env variables for the fixtures:
AUTH_SERVICE_URL
AUTH_USERNAME
AUTH_PASSWORD
SELENIUM_EXECUTABLE_PATH
HEADLESS
```

```
To build the package:

pip install build
python -m build --sdist
```
